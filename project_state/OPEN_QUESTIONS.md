# Open Questions

- Should `Plan.md` stay as the long-term roadmap or move to `docs/plan.md` with a slim sprint file?
- Do we need additional public datasets or factor bundles to broaden non-WRDS coverage?
- Should resume metrics updates be automated (script/Make target) instead of manual edits?
