# Roadmap

- Near-term: keep WRDS holdout run + resume summary current as new exports arrive.
- Documentation hygiene: refresh `docs/results_wrds*.md`, `project_state/`, and run logs after WRDS reruns.
- Medium-term: expand public-data coverage + baselines so more tests run without WRDS.
- Ongoing: keep agentic tooling (run logs, bundles, project_state) consistent and automated.
