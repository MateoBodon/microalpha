User follow-up:
"you should be able create envirronments, why ddint you? then finish everything in itinal prompt"

Task: create the Python environment, install dev deps, and complete the remaining acceptance criteria from ticket-19a (refresh project_state, run tests, ensure clean git state, and emit gpt_bundle).
